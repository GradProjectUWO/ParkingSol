import React from 'react'
import { View, Text } from 'react-native'

const Mapping = () => {
    return (
        <View style={{flex:1, alignItems:'center', justifyContent:'center'}}>
            <Text>This is a Mapping Screen</Text>
        </View>
    )
}
export default Mapping